{
  "name": "vectorstreamtest",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "frontend:dev": "npm --prefix frontend run dev",
    "frontend:build": "npm --prefix frontend run build",
    "compose:up": "docker compose up --build",
    "compose:down": "docker compose down -v"
  }
}
